============
Installation
============

At the command line:

.. code-block:: bash

    $ git clone https://github.com/ajshajib/astrocoffee.git
    $ cd astrocoffee
    $ python setup.py develop --user
